import { VehicleType } from '../core/vehicle.model';

/** El codigo habla ingles; el usuario ve espanol. */
export const VEHICLE_TYPE_LABELS: Record<VehicleType, string> = {
  car: 'Automovil',
  motorcycle: 'Motocicleta',
};
